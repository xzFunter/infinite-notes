version: '3.8'
services:
  whiteboard-app:
    build: .
    container_name: infinite-notes
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production